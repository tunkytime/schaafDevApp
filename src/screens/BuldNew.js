import React from "react";
import { View, Text, StyleSheet } from "react-native";

const BuildNew = () => {
  return (
    <View>
      <Text>Build a new Site</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default BuildNew;
